using System;

namespace VirtualGameMode.Models
{
    [Serializable]
    public enum KeyScope
    {
        AddedApplications = 0,
        FullScreenApplications = 1,
        Global = 2
    }
}
